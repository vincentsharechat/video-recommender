version: '3.8'

services:
  web:
    build: .
    ports:
      - "6006:6006"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - FLASK_SECRET_KEY=${FLASK_SECRET_KEY:-dev-secret-key-change-me}
    volumes:
      - .:/app
    command: python app.py
